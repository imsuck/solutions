#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
typedef unsigned long long ull;
typedef string str;
#define vec vector

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int n;
    cin >> n;
    map<ll, ll> m;

    for (int i = 0; i < n; i++) {
        int a;
        cin >> a;
        m[a]++;
    }
    for (int i = 1; i < n; i++) {
    }

    return 0;
}
